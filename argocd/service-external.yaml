apiVersion: v1
kind: Service
metadata:
  name: argocd-server-external
  namespace: argocd
spec:
  type: ExternalName
  externalName: traefik-public.traefik-public.svc.cluster.local
  ports:
  - port: 80
    targetPort: 80
