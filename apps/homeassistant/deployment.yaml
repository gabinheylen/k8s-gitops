apiVersion: apps/v1
kind: Deployment
metadata:
  name: homeassistant
  namespace: homeassistant
  labels:
    app: homeassistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homeassistant
  template:
    metadata:
      labels:
        app: homeassistant
    spec:
      containers:
      - name: homeassistant
        image: homeassistant/home-assistant:2024.12.1
        ports:
        - containerPort: 8123
          name: http
        env:
        - name: TZ
          value: "Europe/Paris"
        - name: HOMEASSISTANT__HTTP__SERVER_HOST
          value: "0.0.0.0"
        - name: HOMEASSISTANT__HTTP__SERVER_PORT
          value: "8123"
        - name: HOMEASSISTANT__HTTP__CORS_ALLOWED_ORIGINS
          value: "https://homeassistant.local,http://homeassistant.local"
        volumeMounts:
        - name: homeassistant-data
          mountPath: /config
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /onboarding.html
            port: 8123
          initialDelaySeconds: 120
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /onboarding.html
            port: 8123
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
      volumes:
      - name: homeassistant-data
        persistentVolumeClaim:
          claimName: homeassistant-data
